# SpringBoot自动装配

Spring Boot 的自动装配是其核心特性之一，它能根据类路径中的依赖自动配置 Spring 应用。

## 核心原理

### @SpringBootApplication 注解

```java
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}

@SpringBootConfiguration
@EnableAutoConfiguration
@ComponentScan(excludeFilters = { @Filter(type = FilterType.CUSTOM, classes = TypeExcludeFilter.class),
		@Filter(type = FilterType.CUSTOM, classes = AutoConfigurationExcludeFilter.class) })
public @interface SpringBootApplication{
	// ...
}
```

这个注解组合了三个关键注解：

*   `@SpringBootConfiguration` - 实际就是 `@Configuration`
*   `@EnableAutoConfiguration` - 开启自动配置的核心
*   `@ComponentScan` - 组件扫描

### @EnableAutoConfiguration 的工作机制

#### 调用方法链

    启动 Spring Boot 应用
        ↓
    @EnableAutoConfiguration 被处理
        ↓
    AutoConfigurationImportSelector.selectImports()
        ↓
    getAutoConfigurationEntry()
        ↓
    getCandidateConfigurations()
        ↓
    SpringFactoriesLoader.loadFactoryNames(EnableAutoConfiguration.class, classLoader)
        ↓
    loadSpringFactories(classLoader)
        ↓
    classLoader.getResources("META-INF/spring.factories")
        ↓
    遍历所有找到的 URL：
      • spring-boot-autoconfigure-2.x.x.jar!/META-INF/spring.factories
      • my-starter.jar!/META-INF/spring.factories
      • 其他依赖中的 spring.factories
        ↓
    解析 Properties 格式的文件
        ↓
    提取 key = "org.springframework.boot.autoconfigure.EnableAutoConfiguration" 对应的值
        ↓
    返回配置类全限定名列表：
      [WebMvcAutoConfiguration, DataSourceAutoConfiguration, ...]
        ↓
    经过条件过滤（@Conditional）
        ↓
    注册为 Spring Bean

```java
@Import(AutoConfigurationImportSelector.class)
public @interface EnableAutoConfiguration {
    // ...
}

public class AutoConfigurationImportSelector 
    implements DeferredImportSelector, BeanClassLoaderAware {
    
    @Override
    public String[] selectImports(AnnotationMetadata annotationMetadata) {
        // 1. 检查是否启用自动配置
        if (!isEnabled(annotationMetadata)) {
            return NO_IMPORTS;
        }
        // 2. 获取自动配置条目
        AutoConfigurationEntry autoConfigurationEntry = 
            getAutoConfigurationEntry(annotationMetadata);
        return StringUtils.toStringArray(autoConfigurationEntry.getConfigurations());
    }
}

AutoConfigurationImportSelector.getCandidateConfigurations -> SpringFactoriesLoader.loadFactoryNames -> loadSpringFactories
private static Map<String, List<String>> loadSpringFactories(@Nullable ClassLoader classLoader) {
    MultiValueMap<String, String> result = (MultiValueMap)cache.get(classLoader);
    if (result != null) {
        return result;
    } else {
        try {
			// 在这里获取所有 jar 包中的 META-INF/spring.factories 文件
            Enumeration<URL> urls = classLoader != null ? classLoader.getResources("META-INF/spring.factories") : ClassLoader.getSystemResources("META-INF/spring.factories");
            MultiValueMap<String, String> result = new LinkedMultiValueMap();
            cache.put(classLoader, result);
            return result;
        }
    }
}
```

### META-INF/spring.factories 文件内容

```properties
# Spring Boot 自动配置
org.springframework.boot.autoconfigure.EnableAutoConfiguration=\
org.springframework.boot.autoconfigure.web.servlet.WebMvcAutoConfiguration,\
org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
com.example.mystarter.MyCustomAutoConfiguration

# 其他扩展点
org.springframework.context.ApplicationListener=\
com.example.MyApplicationListener
```

> 从 Spring Boot 2.7 开始，spring.factories 被标记为废弃，推荐使用新的方式：META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports

### 条件注解（@Conditional）

条件注解是 Spring Boot 自动装配的核心机制，它允许根据特定条件决定是否创建 Bean 或配置类。

#### @Conditional 基础

```java
@Target({ElementType.TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface Conditional {
    Class<? extends Condition>[] value();
}
```

#### 自定义 Condition

自定义一个linux condition, 在linux环境进行加载

```java
// 自定义条件类
public class LinuxCondition implements Condition {
    @Override
    public boolean matches(ConditionContext context, AnnotatedTypeMetadata metadata) {
        // 获取操作系统名称
        String osName = context.getEnvironment().getProperty("os.name");
        return osName != null && osName.toLowerCase().contains("linux");
    }
}

// 使用自定义条件
@Configuration
public class MyConfig {
    @Bean
    @Conditional(LinuxCondition.class)
    public LinuxService linuxService() {
        return new LinuxService();
    }
}
```

| @Conditional扩展注解                | 作用（判断是否满足当前指定条件）               |
| ------------------------------- | ------------------------------ |
| @ConditionalOnJava              | 系统的java版本是否符合要求                |
| @ConditionalOnBean              | 容器中存在指定Bean；                   |
| @ConditionalOnMissingBean       | 容器中不存在指定Bean；                  |
| @ConditionalOnExpression        | 满足SpEL表达式指定                    |
| @ConditionalOnClass             | 系统中有指定的类                       |
| @ConditionalOnMissingClass      | 系统中没有指定的类                      |
| @ConditionalOnSingleCandidate   | 容器中只有一个指定的Bean，或者这个Bean是首选Bean |
| @ConditionalOnProperty          | 系统中指定的属性是否有指定的值                |
| @ConditionalOnResource          | 类路径下是否存在指定资源文件                 |
| @ConditionalOnWebApplication    | 当前是web环境                       |
| @ConditionalOnNotWebApplication | 当前不是web环境                      |
| @ConditionalOnJndi              | JNDI存在指定项                      |

# SpringBoot的属性自动绑定

属性自动绑定是 Spring Boot 将配置文件（application.properties/yml）中的属性值自动绑定到 Java 对象的核心机制。

## 核心注解

### @ConfigurationProperties

```java
@ConfigurationProperties(prefix = "my.app")
public class MyAppProperties {
    private String name;
    private int port;
    private List<String> servers;
    private Map<String, String> config;
    
    // getters and setters
}
```

### @EnableConfigurationProperties

```java
@SpringBootApplication
@EnableConfigurationProperties(MyAppProperties.class)
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

## 绑定方式

### 方式一：@ConfigurationProperties + @Component

```java
@Component
@ConfigurationProperties(prefix = "my.app")
public class AppConfig {
    private String name;
    private int version;
    
    // getters/setters
}
```

### 方式二：@ConfigurationProperties + @Bean

```java
@Configuration
public class AppConfig {
    
    @Bean
    @ConfigurationProperties(prefix = "my.app")
    public MyAppProperties myAppProperties() {
        return new MyAppProperties();
    }
}
```

### 方式三：@ConfigurationPropertiesScan

```java
@SpringBootApplication
@ConfigurationPropertiesScan("com.example.config")
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

### 默认值和占位符

```java
@ConfigurationProperties(prefix = "my.app")
public class DefaultValueProperties {
    // 通过字段默认值
    private String name = "default-app";
    private int port = 8080;
    
    // 通过注解
    @Value("${my.app.timeout:30s}")
    private Duration timeout;
    
    // 通过构造函数
    private List<String> servers = new ArrayList<>();
    
    // getters/setters
}
```

```yaml
my:
  app:
    name: MyApplication
    full-name: ${my.app.name}-Service
    port: 8080
    url: http://localhost:${my.app.port}
    
    # 引用系统属性
    user-home: ${user.home}
    java-version: ${java.version}
    
    # 默认值
    optional-value: ${UNDEFINED_VALUE:default-value}
```

## 属性提示工具 spring-boot-configuration-processor

这是一个用于提升开发体验的可选工具，需要你手动添加到项目依赖中。它的核心作用是编译时为 @ConfigurationProperties 注解的类生成配置元数据，让 IDE 在编写 application.properties 或 application.yml 时提供自动补全和提示功能。

```xml
<!--你需要在项目的构建文件中显式引入，通常将其依赖范围设为 optional 即可-->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-configuration-processor</artifactId>
    <optional>true</optional>
</dependency>
```

导入后编译就会生成元数据文件, maven项目在 target/classes/META-INF/spring-configuration-metadata.json 。

```json
{
  "groups": [
    {
      "name": "myapp",
      "type": "com.example.MyAppProperties",
      "sourceType": "com.example.MyAppProperties"
    }
  ],
  "properties": [
    {
      "name": "myapp.name",
      "type": "java.lang.String",
      "sourceType": "com.example.MyAppProperties"
    },
    {
      "name": "myapp.version",
      "type": "int",
      "sourceType": "com.example.MyAppProperties"
    }
  ],
  "hints": []
}

```

### 自定义元数据

如果需要补充或覆盖自动生成的元数据，可以手动创建 additional-spring-configuration-metadata.json 文件，并放置在 src/main/resources/META-INF/ 目录下。例如：

```json
{
  "properties": [
    {
      "name": "myapp.name",
      "description": "应用名称，建议使用全称",
      "type": "java.lang.String",
      "defaultValue": "DefaultApp"
    }
  ]
}
```

# SpringBoot启动流程

## 启动入口

```java
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

## 整体流程图

    main() 启动
        ↓
    SpringApplication.run()
        ↓
    new SpringApplication()  → 推断应用类型 → 加载初始化器 → 加载监听器
        ↓
    run() 方法
        ↓
    创建 StopWatch 计时
        ↓
    配置 Headless 属性
        ↓
    加载 SpringApplicationRunListeners
        ↓
    Environment 准备 → 加载配置文件 → 绑定属性
        ↓
    打印 Banner
        ↓
    创建 ApplicationContext
        ↓
    准备 ApplicationContext
        ↓
    刷新 ApplicationContext（核心）
        ↓
    刷新后处理
        ↓
    执行 Runner
        ↓
    启动完成

## 第一阶段：SpringApplication 实例化

```java
public SpringApplication(ResourceLoader resourceLoader, Class<?>... primarySources) {
    this.resourceLoader = resourceLoader;
    Assert.notNull(primarySources, "PrimarySources must not be null");
    this.primarySources = new LinkedHashSet<>(Arrays.asList(primarySources));
    
    // 1. 推断 Web 应用类型
    this.webApplicationType = WebApplicationType.deduceFromClasspath();
    
    // 2. 加载所有 META-INF/spring.factories 中的 ApplicationContextInitializer
    this.setInitializers(this.getSpringFactoriesInstances(
        ApplicationContextInitializer.class));
    
    // 3. 加载所有 META-INF/spring.factories 中的 ApplicationListener
    this.setListeners(this.getSpringFactoriesInstances(
        ApplicationListener.class));
    
    // 4. 推断主启动类
    this.mainApplicationClass = this.deduceMainApplicationClass();
}
```

## 第二阶段：推断 Web 应用类型

```java
// WebApplicationType.deduceFromClasspath()
static WebApplicationType deduceFromClasspath() {
    // 存在 Reactive 相关类
    if (ClassUtils.isPresent("org.springframework.web.reactive.DispatcherHandler", null)
            && !ClassUtils.isPresent("org.springframework.web.servlet.DispatcherServlet", null)) {
        return WebApplicationType.REACTIVE;
    }
    
    // 存在 Servlet 相关类
    for (String className : SERVLET_INDICATOR_CLASSES) {
        if (!ClassUtils.isPresent(className, null)) {
            return WebApplicationType.NONE;
        }
    }
    
    return WebApplicationType.SERVLET;
}
```

## 第三阶段：run() 方法核心流程

```java
public ConfigurableApplicationContext run(String... args) {
    // 1. 启动计时器
    StopWatch stopWatch = new StopWatch();
    stopWatch.start();
    
    // 2. 创建启动上下文
    DefaultBootstrapContext bootstrapContext = createBootstrapContext();
    ConfigurableApplicationContext context = null;
    
    // 3. 配置 Headless 模式
    configureHeadlessProperty();
    
    // 4. 获取并启动 SpringApplicationRunListeners
    SpringApplicationRunListeners listeners = getRunListeners(args);
    listeners.starting(bootstrapContext, this.mainApplicationClass);
    
    try {
        // 5. 准备 Environment
        ApplicationArguments applicationArguments = new DefaultApplicationArguments(args);
        ConfigurableEnvironment environment = prepareEnvironment(listeners, bootstrapContext, applicationArguments);
        configureIgnoreBeanInfo(environment);
        
        // 6. 打印 Banner
        Banner printedBanner = printBanner(environment);
        
        // 7. 创建 ApplicationContext
        context = createApplicationContext();
        context.setApplicationStartup(this.applicationStartup);
        
        // 8. 准备 ApplicationContext
        prepareContext(bootstrapContext, context, environment, listeners, applicationArguments, printedBanner);
        
        // 9. 刷新 ApplicationContext（核心）
        refreshContext(context);
        
        // 10. 刷新后处理
        afterRefresh(context, applicationArguments);
        
        // 11. 停止计时
        stopWatch.stop();
        
        // 12. 发布启动完成事件
        listeners.started(context);
        
        // 13. 执行 Runner
        callRunners(context, applicationArguments);
        
        // 14. 发布运行中事件
        listeners.running(context);
    } catch (Throwable ex) {
        handleRunFailure(context, ex, listeners);
        throw new IllegalStateException(ex);
    }
    
    return context;
}
```

### Environment 准备

```java
private ConfigurableEnvironment prepareEnvironment(
        SpringApplicationRunListeners listeners,
        DefaultBootstrapContext bootstrapContext,
        ApplicationArguments applicationArguments) {
    
    // 创建 Environment
    ConfigurableEnvironment environment = getOrCreateEnvironment();
    
    // 配置 Environment
    configureEnvironment(environment, applicationArguments.getSourceArgs());
    
    // 发布 Environment 准备完成事件
    listeners.environmentPrepared(bootstrapContext, environment);
    
    // 绑定 SpringApplication 到 Environment
    bindToSpringApplication(environment);
    
    // 转换 Environment（如果需要）
    if (!this.isCustomEnvironment) {
        environment = new EnvironmentConverter(getClassLoader()).convertEnvironmentIfNecessary(environment, deduceEnvironmentClass());
    }
    
    // 附加配置属性源
    ConfigurationPropertySources.attach(environment);
    
    return environment;
}
```

### 创建 ApplicationContext

```java
protected ConfigurableApplicationContext createApplicationContext() {
    // 根据 Web 类型创建不同的 ApplicationContext
    switch (this.webApplicationType) {
        case SERVLET:
            return new AnnotationConfigServletWebServerApplicationContext();
        case REACTIVE:
            return new AnnotationConfigReactiveWebServerApplicationContext();
        default:
            return new AnnotationConfigApplicationContext();
    }
}
```

### 准备 Context

```java
private void prepareContext(DefaultBootstrapContext bootstrapContext,
        ConfigurableApplicationContext context,
        ConfigurableEnvironment environment,
        SpringApplicationRunListeners listeners,
        ApplicationArguments applicationArguments,
        Banner printedBanner) {
    
    // 设置 Environment
    context.setEnvironment(environment);
    
    // 后置处理 Context
    postProcessApplicationContext(context);
    
    // 执行所有 Initializer
    applyInitializers(context);
    
    // 发布 Context 准备完成事件
    listeners.contextPrepared(context);
    
    // 注册启动参数 Bean
    context.getBeanFactory().registerSingleton("springApplicationArguments", applicationArguments);
    
    // 注册 Banner Bean
    if (printedBanner != null) {
        context.getBeanFactory().registerSingleton("springBootBanner", printedBanner);
    }
    
    // 获取主配置类
    Set<Object> sources = getAllSources();
    Assert.notEmpty(sources, "Sources must not be empty");
    
    // 加载主配置类
    load(context, sources.toArray(new Object[0]));
    
    // 发布 Context 加载完成事件
    listeners.contextLoaded(context);
}
```

### 刷新 Context（核心）

```java
// AbstractApplicationContext.refresh()
public void refresh() throws BeansException, IllegalStateException {
    synchronized (this.startupShutdownMonitor) {
        // 1. 准备刷新上下文
        prepareRefresh();
        
        // 2. 获取 BeanFactory
        ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();
        
        // 3. 准备 BeanFactory
        prepareBeanFactory(beanFactory);
        
        try {
            // 4. BeanFactory 后置处理
            postProcessBeanFactory(beanFactory);
            
            // 5. 调用 BeanFactoryPostProcessor
            invokeBeanFactoryPostProcessors(beanFactory);
            
            // 6. 注册 BeanPostProcessor
            registerBeanPostProcessors(beanFactory);
            
            // 7. 初始化 MessageSource
            initMessageSource();
            
            // 8. 初始化 ApplicationEventMulticaster
            initApplicationEventMulticaster();
            
            // 9. 初始化特定子类（如嵌入式 Web 服务器）
            onRefresh();
            
            // 10. 注册监听器
            registerListeners();
            
            // 11. 实例化所有单例 Bean（非懒加载） 在这里进行了bean的创建 aop的代理增强
            finishBeanFactoryInitialization(beanFactory);
            
            // 12. 完成刷新，发布事件
            finishRefresh();
        } catch (BeansException ex) {
            // 销毁已创建的 Bean
            destroyBeans();
            cancelRefresh(ex);
            throw ex;
        } finally {
            resetCommonCaches();
        }
    }
}
```

### 嵌入式 Web 服务器启动

```java
// ServletWebServerApplicationContext.onRefresh()
protected void onRefresh() {
    super.onRefresh();
    try {
        // 创建嵌入式 Web 服务器
        createWebServer();
    } catch (Throwable ex) {
        throw new ApplicationContextException("Unable to start web server", ex);
    }
}

// 创建 Web 服务器
private void createWebServer() {
    ServletWebServerFactory factory = getWebServerFactory();
    this.webServer = factory.getWebServer(getSelfInitializer());
    
    // 启动 Web 服务器在 finishRefresh() 中调用
}
```

### 执行 Runner

```java
private void callRunners(ApplicationContext context, ApplicationArguments args) {
    List<Object> runners = new ArrayList<>();
    
    // 获取所有 ApplicationRunner
    runners.addAll(context.getBeansOfType(ApplicationRunner.class).values());
    
    // 获取所有 CommandLineRunner
    runners.addAll(context.getBeansOfType(CommandLineRunner.class).values());
    
    // 排序（@Order 或 Ordered 接口）
    AnnotationAwareOrderComparator.sort(runners);
    
    // 执行
    for (Object runner : runners) {
        if (runner instanceof ApplicationRunner) {
            ((ApplicationRunner) runner).run(args);
        }
        if (runner instanceof CommandLineRunner) {
            ((CommandLineRunner) runner).run(args.getSourceArgs());
        }
    }
}
```

## 事件发布顺序

```java
// Spring Boot 启动过程中的事件顺序
1. ApplicationStartingEvent          // 应用启动开始
2. ApplicationEnvironmentPreparedEvent // Environment 准备完成
3. ApplicationContextInitializedEvent // Context 初始化完成
4. ApplicationPreparedEvent          // Context 准备完成
5. ApplicationStartedEvent           // Context 刷新完成
6. ApplicationReadyEvent             // 应用就绪（Runner 执行后）
7. ApplicationFailedEvent            // 启动失败（异常时）
```

## 自定义扩展点

### ApplicationContextInitializer 初始化给定的应用上下文

`ApplicationContextInitializer` 是 Spring 框架提供的一个回调接口，用于在 Spring 容器（ApplicationContext）刷新（refresh）之前对容器进行自定义初始化操作。

```java
public class MyInitializer implements ApplicationContextInitializer<ConfigurableApplicationContext> {
    @Override
    public void initialize(ConfigurableApplicationContext context) {
        // Context 创建后、刷新前执行
    }
}
```

1.  时机最早：在容器刷新前执行，可以影响整个容器的行为
2.  影响全局：一次配置，全局生效
3.  轻量级：不依赖其他 Bean，避免循环依赖
4.  灵活性强：可以动态改变容器行为

#### 应用场景

| 场景类别       | 具体应用                 | 典型示例          |
| :--------- | :------------------- | :------------ |
| **环境配置**   | Profile激活、配置加载、属性源管理 | 动态环境切换、多环境配置  |
| **配置中心**   | Apollo/Nacos集成、数据库配置 | 远程配置拉取、动态刷新   |
| **Bean管理** | 条件注册、插件加载、动态代理       | 插件化架构、多实现切换   |
| **监控运维**   | 启动监控、性能追踪、日志集成       | 启动耗时统计、MDC注入  |
| **安全加密**   | 配置解密、敏感信息脱敏          | 数据库密码解密、日志脱敏  |
| **服务治理**   | 服务注册、灰度发布、流量染色       | Consul集成、灰度路由 |
| **测试支持**   | Mock替换、测试数据初始化       | 单元测试、集成测试     |

### SpringApplicationRunListener 监听器

```java
public class MyRunListener implements SpringApplicationRunListener {
    
    public MyRunListener(SpringApplication application, String[] args) {
        // 必须要有这个构造函数
    }
    
	// 应用开始启动时，最早执行的阶段
    @Override
    public void starting(ConfigurableBootstrapContext bootstrapContext, Class<?> mainApplicationClass) {
        // 启动时执行 比如打印一些参数 校验java版本或者检查端口有没有
        System.out.println("========================================");
        System.out.println("应用启动中...");
        System.out.println("主类: " + mainApplicationClass.getName());
        System.out.println("PID: " + ProcessHandle.current().pid());
        System.out.println("启动时间: " + new Date());
        System.out.println("JVM 最大内存: " + Runtime.getRuntime().maxMemory() / 1024 / 1024 + "MB");
        System.out.println("========================================");

        // 检查 Java 版本
        if (!System.getProperty("java.version").startsWith("17")) {
            throw new RuntimeException("需要 Java 17 或更高版本");
        }
    }
    
	// 环境准备完成，配置已加载但容器未创建
    @Override
    public void environmentPrepared(ConfigurableBootstrapContext bootstrapContext, ConfigurableEnvironment environment) {
        // 从外部服务加载配置
        String configServer = environment.getProperty("config.server.url");
        if (configServer != null) {
            Map<String, Object> remoteConfig = fetchRemoteConfig(configServer);
            environment.getPropertySources()
                .addFirst(new MapPropertySource("remoteConfig", remoteConfig));
        }

		// 验证必要配置
        String[] requiredProps = {"spring.datasource.url", "spring.datasource.username"};
        for (String prop : requiredProps) {
            if (!environment.containsProperty(prop)) {
                throw new IllegalStateException("缺少必要配置: " + prop);
            }
        }
		
    }
    
	// ApplicationContext 创建完成，Bean 尚未加载
    @Override
    public void contextPrepared(ConfigurableApplicationContext context) {
        // Context 准备完成
    }
    
	// 配置类已加载，但容器尚未刷新
    @Override
    public void contextLoaded(ConfigurableApplicationContext context) {
        // Context 加载完成
    }
    
	// 容器启动完成，但 Runner 尚未执行
    @Override
    public void started(ConfigurableApplicationContext context, Duration timeTaken) {
        // 场景1：预热数据库连接池
        DataSource dataSource = context.getBean(DataSource.class);
        if (dataSource instanceof HikariDataSource) {
            ((HikariDataSource) dataSource).getConnection().close();
        }
        
        // 场景2：加载缓存
        CacheManager cacheManager = context.getBean(CacheManager.class);
        cacheManager.getCacheNames().forEach(cacheName -> {
            System.out.println("预热缓存: " + cacheName);
            // 预加载常用数据
            loadCacheData(cacheManager.getCache(cacheName));
        });
        
        // 场景3：初始化线程池
        ExecutorService executor = context.getBean("taskExecutor", ExecutorService.class);
        executor.submit(() -> {});
    }
    
    @Override
    public void running(ConfigurableApplicationContext context, Duration timeTaken) {
        // 运行中
    }
    
    @Override
    public void failed(ConfigurableApplicationContext context, Throwable exception) {
        // 启动失败
    }
}
```

#### 也可以自定义Listener去监听指定的事件进行操作

```java
@Slf4j
@Component
public class DeviceListener {

    @EventListener(ApplicationStartedEvent.class)
    public void onApplicationReady() {
        log.info("项目启动, 初始化海康设备信息");
        FaceMain.init();
        log.info("海康设备信息初始化完成");
    }

    @EventListener(ContextClosedEvent.class)
    public void onContextClosed() {
        log.info("项目销毁, 销毁海康设备信息");
        FaceMain.destroy();
    }
}
```

## 启动流程时序图

```java
main()  // Spring Boot 应用主入口
  │
  ├─→ new SpringApplication()  // 【阶段1：创建 SpringApplication 实例】
  │     ├─→ deduceWebApplicationType()  // 推断应用类型（SERVLET/REACTIVE/NONE）
  │     │   └─→ 检查 ClassPath 中是否存在 DispatcherServlet、DispatcherHandler 等类
  │     ├─→ load Initializers  // 加载所有 ApplicationContextInitializer
  │     │   └─→ 从 META-INF/spring.factories 中读取配置，实例化并排序
  │     └─→ load Listeners     // 加载所有 ApplicationListener
  │         └─→ 从 META-INF/spring.factories 中读取配置，实例化并排序
  │
  └─→ run()  // 【阶段2：执行应用启动】
        │
        ├─→ SpringApplicationRunListeners.starting()  // 【事件1：启动开始】
        │   └─→ 通知所有监听器应用开始启动，可用于环境检查、启动日志等
        │
        ├─→ prepareEnvironment()  // 【阶段3：准备环境】
        │     ├─→ create Environment  // 创建 ConfigurableEnvironment
        │     │   └─→ 根据 Web 类型创建 StandardServletEnvironment 或 StandardEnvironment
        │     └─→ listeners.environmentPrepared()  // 【事件2：环境准备完成】
        │         └─→ 通知监听器环境已就绪，此时可动态添加配置源、激活 Profile
        │
        ├─→ printBanner()  // 【阶段4：打印 Banner】
        │   └─→ 根据配置在控制台或日志中打印 Spring Boot 启动图案
        │
        ├─→ createApplicationContext()  // 【阶段5：创建 IoC 容器】
        │   └─→ 根据 Web 类型创建对应的 ApplicationContext
        │       ├─→ SERVLET → AnnotationConfigServletWebServerApplicationContext
        │       ├─→ REACTIVE → AnnotationConfigReactiveWebServerApplicationContext
        │       └─→ NONE → AnnotationConfigApplicationContext
        │
        ├─→ prepareContext()  // 【阶段6：准备容器】
        │     ├─→ applyInitializers()  // 执行所有 ApplicationContextInitializer
        │     │   └─→ 回调每个 Initializer 的 initialize() 方法，在刷新前定制容器
        │     ├─→ listeners.contextPrepared()  // 【事件3：容器准备完成】
        │     │   └─→ 通知监听器容器已创建但 Bean 定义尚未加载
        │     ├─→ load sources  // 加载主配置类
        │     │   └─→ 将 @SpringBootApplication 标记的类作为配置源加载
        │     └─→ listeners.contextLoaded()  // 【事件4：容器加载完成】
        │         └─→ 通知监听器配置类已加载，Bean 定义已注册但未实例化
        │
        ├─→ refreshContext()  // 【阶段7：刷新容器（核心）】
        │     │
        │     ├─→ prepareRefresh()  // 刷新前准备
        │     │   └─→ 设置启动时间、验证必要属性、初始化早期事件监听器
        │     │
        │     ├─→ obtainFreshBeanFactory()  // 获取 BeanFactory
        │     │   └─→ 返回 DefaultListableBeanFactory 实例
        │     │
        │     ├─→ invokeBeanFactoryPostProcessors()  // 执行 BeanFactory 后置处理器
        │     │   ├─→ 调用 BeanDefinitionRegistryPostProcessor
        │     │   ├─→ 调用 BeanFactoryPostProcessor
        │     │   └─→ ★ 自动配置核心在这里执行 ★
        │     │       └─→ ConfigurationClassPostProcessor 处理 @Configuration 类
        │     │           └─→ AutoConfigurationImportSelector 加载自动配置类
        │     │               └─→ 根据 @Conditional 条件注解筛选配置
        │     │
        │     ├─→ registerBeanPostProcessors()  // 注册 Bean 后置处理器
        │     │   └─→ 注册所有 BeanPostProcessor，用于 Bean 实例化后的拦截处理
        │     │
        │     ├─→ onRefresh()  // 容器子类扩展点
        │     │   └─→ ★ 创建嵌入式 Web 服务器 ★
        │     │       ├─→ SERVLET → 创建 Tomcat/Jetty/Undertow 服务器
        │     │       └─→ REACTIVE → 创建 Netty 服务器
        │     │
        │     ├─→ finishBeanFactoryInitialization()  // 实例化所有非懒加载的单例 Bean
        │     │   ├─→ 转换服务初始化
        │     │   ├─→ 冻结 Bean 定义
        │     │   └─→ 逐个实例化 Bean（依赖注入、初始化、AOP 代理）
        │     │
        │     └─→ finishRefresh()  // 完成刷新
        │           ├─→ 初始化生命周期处理器
        │           ├─→ 发布 ContextRefreshedEvent 事件
        │           └─→ ★ 启动嵌入式 Web 服务器 ★
        │               └─→ 启动 Tomcat/Jetty/Netty 监听端口
        │
        ├─→ listeners.started()  // 【事件5：容器刷新完成】
        │   └─→ 通知监听器容器已启动，此时可进行服务预热、健康检查
        │
        ├─→ callRunners()  // 【阶段8：执行 Runner】
        │     ├─→ ApplicationRunner  // 执行所有 ApplicationRunner（包装 ApplicationArguments）
        │     │   └─→ 按 @Order 顺序执行 run() 方法，常用于初始化业务数据
        │     └─→ CommandLineRunner  // 执行所有 CommandLineRunner（原始参数数组）
        │         └─→ 按 @Order 顺序执行 run() 方法，常用于初始化业务数据
        │
        └─→ listeners.running()  // 【事件6：应用就绪】
            └─→ 通知监听器应用已完全启动，可执行后台任务、服务注册
```

## AOP代理时机

    ┌─────────────────────────────────────────────────────────────────┐
    │  registerBeanPostProcessors                                      │
    │  └── 注册了 AnnotationAwareAspectJAutoProxyCreator              │
    │      （这是一个 BeanPostProcessor，但还没执行）                   │
    └─────────────────────────────────────────────────────────────────┘
                                  ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │  finishBeanFactoryInitialization                                 │
    │  └── 创建 UserService                                            │
    │      ├── 1. 实例化：new UserService() → 原始对象                  │
    │      ├── 2. 依赖注入：填充属性                                    │
    │      └── 3. 初始化：                                             │
    │          └── 调用 BeanPostProcessor.postProcessAfterInitialization│
    │              └── AnnotationAwareAspectJAutoProxyCreator          │
    │                  └── 检测到 @Log 注解                            │
    │                  └── 创建代理对象（CGLIB）                        │
    │                  └── 返回代理对象 ← 替换原始对象                   │
    └─────────────────────────────────────────────────────────────────┘
                                  ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │  单例池中存储的是代理对象                                          │
    │  调用 userService.doSomething() 时，实际执行的是代理的逻辑         │
    └─────────────────────────────────────────────────────────────────┘

